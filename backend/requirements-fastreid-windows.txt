# Install after `pip install -r requirements.txt`
# FastReID is kept optional because Windows environments vary and some users run CPU-only.
fastreid==1.4.0
faiss-cpu>=1.7.3,<2.0.0
termcolor>=2.2.0,<3.0.0
yacs>=0.1.8,<0.2.0
